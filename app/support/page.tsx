import { AppShell } from "@/components/app-shell";

export default function SupportPage() {
  return (
    <AppShell>
      <section className="page-section">
        <h1>Support</h1>
        <p>Contact Forge Housing support for help with projects, listings, and maintenance.</p>
      </section>
    </AppShell>
  );
}
